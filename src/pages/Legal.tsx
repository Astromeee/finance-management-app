import { Link } from 'react-router-dom'

export function LegalPage({ kind }: { kind: 'privacy' | 'terms' }) {
  const privacy = kind === 'privacy'
  const supportEmail = (import.meta.env.VITE_SUPPORT_EMAIL as string | undefined) ?? 'a.moeed1875@gmail.com'
  const operatorName = (import.meta.env.VITE_LEGAL_OPERATOR_NAME as string | undefined) ?? 'Abdul Moeed'
  return <main className="min-h-screen bg-[var(--bg-deep)] p-5 text-white"><article className="card mx-auto max-w-2xl p-6 sm:p-8"><Link className="text-sm font-semibold text-[var(--accent)]" to="/login">← Back to Pocket Ledger</Link><h1 className="mt-5 text-3xl font-semibold">{privacy ? 'Privacy Policy' : 'Terms of Use'}</h1><p className="mt-2 text-sm text-[var(--muted)]">Public Beta draft · Last updated 14 July 2026</p>{privacy ? <div className="mt-6 space-y-4 text-sm leading-7 text-[var(--muted)]"><p>Pocket Ledger is operated by {operatorName}. It stores the profile and financial records you choose to enter so the app can provide accounts, budgets, goals, debts, and reports. It does not connect to your bank.</p><p>Authentication and database hosting are provided through Supabase. Access is restricted to your authenticated user ID using database row-level security.</p><p>You can export your data or delete your account from Settings. Account deletion removes the active ledger, authentication account, and associated diagnostic events. A protected migration backup is retained temporarily for beta recovery and will not receive new writes.</p><p>We do not sell financial data. Authenticated crash diagnostics contain only an error type, a non-personal route label, app version, user identifier, and timestamp. They never contain balances, notes, transaction titles, exports, stacks, messages, or email addresses and are automatically deleted after 30 days.</p><p>Privacy questions or deletion issues: <a className="text-[var(--accent)]" href={`mailto:${supportEmail}`}>{supportEmail}</a>.</p></div> : <div className="mt-6 space-y-4 text-sm leading-7 text-[var(--muted)]"><p>Pocket Ledger is operated by {operatorName} as a public beta for personal record keeping. It is not a bank, payment service, accountant, financial adviser, or source of tax advice.</p><p>You are responsible for checking entries, exports, balances, and reports before relying on them. Do not use the app for illegal activity or attempt to access another user’s data.</p><p>Beta features may change. We will take reasonable steps to protect and preserve data, but you should keep periodic exports of important records.</p><p>You may stop using the service and delete your account at any time from Settings. Questions may be sent to <a className="text-[var(--accent)]" href={`mailto:${supportEmail}`}>{supportEmail}</a>.</p></div>}</article></main>
}
